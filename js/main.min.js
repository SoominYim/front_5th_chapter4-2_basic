function showTopBar() {
  const e = "France",
    t = 20;
  setTimeout(() => {
    const n = document.querySelector("section.country-bar");
    n && ((n.innerHTML = `<p>Orders to <b>${e}</b> are subject to <b>${t}%</b> VAT</p>`), n.classList.remove("hidden"));
  }, 1e3);
}
showTopBar();
